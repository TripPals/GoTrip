<%= render 'layouts/header_trip' %>
<div class="planMessage"></div>
<div id="planView" data-controller="plan">

<% if current_user.current_trip_role(params[:trip_id]) == "viewer" %>
  <div id="PlanPageView"></div>
<% else %>  
  <div id="spotPlanningSection"></div>
<% end%>
  <div id="googleMapSection" 
       class="mapInPlanPage"
       data-action="mapsLoaded@document->plan#renderMap"
       data-plan-target="initialmap"
  ></div>
</div>
<%= javascript_pack_tag 'trip_body_vue' %>