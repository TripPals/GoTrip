<h1>邀請朋友頁</h1>
<%= form_for(@user_trip_role, url: rights_update_path) do |form| %>
 <div class="fields">
    <%= form.label :role, "權限" %>
    <%= form.select :role, options_from_collection_for_select(UserTrip.all, :role, :role), {include_blank: true}%>
  <%#= form.label :role, "權限" %>
  <%#= form.select :role, options_from_collection_for_select(UserTrip.all, :role, :role), {include_blank: true}%>
  <%= form.submit %>
 </div>
<% end %>
<% @users.each do |user| %>
<div>
    <%= user.current_trip_role(params[:trip_id]) %>
</div>
<div>
    <%= user.name %>
</div>
<div>
    <%= user.email %>
</div>

<% end %>
